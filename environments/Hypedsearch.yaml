name: Hypedsearch
channels:
  - conda-forge
  - pytorch
  - bioconda
  - defaults
dependencies:
  - python>=3.6
  - anaconda
  - numpy
  - pip
  - pip:
    - pyteomics
    - memory_profiler
    - numpy
    - pyteomics
    - lxml
    - argparse
    - pandas